# Data Folder

Place your test PDF here as `sample.pdf` — this file is used by:
- `eval/chunking_experiment.py` (reads `data/sample.pdf` directly)
- Your manual testing when writing `eval/eval_questions.json`

Any additional PDFs you want to demo with during the live presentation can also go here, but only `sample.pdf` is required for the evaluation scripts to run.
